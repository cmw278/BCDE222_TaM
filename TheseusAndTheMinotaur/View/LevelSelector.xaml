<Page
    x:Class="TheseusAndTheMinotaur.LevelSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TheseusAndTheMinotaur"
    xmlns:muxc="using:Windows.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="LevelItem">
            <Button Style="{StaticResource ButtonRevealStyle}"
                    Background="{StaticResource BrownAcrylic}"
                    Content="{Binding}"
                    Click="LevelItem_Click"
                    FontSize="48"
                    Width="250" Height="250" />
        </DataTemplate>
    </Page.Resources>

    <ScrollViewer>
        <GridView HorizontalAlignment="Center"
                  ItemTemplate="{StaticResource LevelItem}"
                  ItemsSource="{x:Bind LevelNames}">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid MaximumRowsOrColumns="5" Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
        </GridView>
    </ScrollViewer>
</Page>
